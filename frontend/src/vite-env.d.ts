/// <reference types="svelte" />
/// <reference types="vite/client" />

// Build-time constant injected by Vite's `define` (see
// vite.config.js) from the crate version in Cargo.toml.
declare const __APP_VERSION__: string;
